Kruskal's MST algorithm using DSU with union by size.

\textbf{Time Complexity:} $O(E \log E)$

\textbf{Space Complexity:} $O(V + E)$

Same as Kruskal by rank, but uses actual component sizes instead of rank.
