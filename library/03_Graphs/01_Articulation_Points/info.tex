Articulation Points are vertices whose removal increases the number of connected components.

\textbf{Time Complexity:} $O(V + E)$

\textbf{Algorithm:} Uses DFS with discovery and low times to identify cut vertices.
